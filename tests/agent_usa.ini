[general]
system = Apple2
name: Agent Usa

[entries]
;file, start *, action, call address or custom opcodes *, silence(ms)
; * in HEXA
; actions:
; Jump, Call, Nothing, Return, Custom
entry = agent_usa#060801,801,Jump,801,0

